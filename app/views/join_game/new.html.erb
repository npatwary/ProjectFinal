

<h1>Select Game Attributes for <%= label_tag @accepted_game.name %> </h1>

<br>
<br>


<%= form_tag(join_game_path, :method=> 'post' ) do %>

 
 <h4> <%= label_tag 'Select Player Character'   %>  </h4>  
  
  
  <div class="form_row">
          <% @user_player_characters.each do |pc| %>
          <br><%= radio_button_tag 'pc_id', pc.id, @pc == pc %>
          <%= label_tag "#{pc.name}", pc.name.humanize %>
        <% end %>
  </div>

 

<br>

<div class="col-sm-10">
      <strong>Insert key</strong>
      <%= password_field_tag :passkey %>
</div>

<br>
     
     <br> <br>
     <%= hidden_field_tag "inv_id", @invitation_id   %>
     <%= hidden_field_tag "game_id", @game_id  %>

          <%= submit_tag "Join"  %>
          <%= submit_tag "Cancel" %> 
    
<% end %>







<hr>
<p> ---------------------Please ignore below this line-----------------------</p>

<br>
<br>
<%= @accepted_game.name  %>
<%= @accepted_invited.game_password  %>
<br>
<%= @invitation_id %>
<%= @game_id %>


<br>
<br>
<h4> Listing all invited games </h4>

<table>
<tr>
    <th>Invited games</th>
    <th>Password</th>
 </tr>
<% @game_invitations_received.each do |game_invitation| %>
  <tr>
  	<td><%= game_invitation.game.name %> </td>
    <td><%= game_invitation.game_password %></td>
    <br> 
  </tr>
 <% end %>
</table>

<h4> Listing all player characters </h4> 
<% @player_characters.each do |player_character| %>
    <%= player_character.name %> <br>
 <% end %>
 
<h4> Listing current user player characters </h4> 
<% @user_player_characters.each do |user_player_character| %>
    <%= user_player_character.name %> <br>
 <% end %>

  <div class="dropdown">
  <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-expanded="true">
    Dropdown
    <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
    <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Action</a></li>
    <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Another action</a></li>
    <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Something else here</a></li>
    <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Separated link</a></li>
  </ul>
</div>



  <div class="col-sm-12">
  <hr>
  <%= link_to "Go to User Home page",user_path(current_user) %>
</div>